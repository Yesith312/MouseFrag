android.useAndroidX=true
android.nonTransitiveRClass=true

# NOTA: el override de aapt2 para Termux (arquitectura ARM) NO va aqui,
# va en tu ~/.gradle/gradle.properties personal (fuera del repo), porque
# la ruta cambia segun cada celular. Revisa el README.md para el comando
# exacto.
